# 加载响应与流量优化规划方案

## 1. 目标

本方案用于降低公网访问时的图片流量、首屏加载时间和后端响应压力，同时不破坏当前访问密码鉴权、数据空间隔离、后台任务执行、分镜实时反馈和原图预览下载能力。

核心目标：

- 列表页、历史页、图库页默认不再加载原图。
- 页面刷新和切换会话时减少无效 API 请求。
- 云服务器上由更适合的层承接静态文件和图片分发。
- 长期历史数据变多后，任务、图库和会话加载仍保持稳定。

## 2. 当前现状

已具备的能力：

- `/media/...` 和 `/assets/...` 已增加浏览器私有缓存头。
- 前端图片节点已使用 `loading="lazy"` 和 `decoding="async"`。
- 对话和分镜任务已有任务级 SSE，轮询作为兜底。
- 分镜规划与图片展示已避免重复渲染，并能把图片插入对应镜头位置。

主要瓶颈：

- 图片列表、图库、历史和分镜卡片仍可能使用原图 URL，只是延后加载。
- `/api/tasks`、`/api/gallery`、会话详情等接口会随着历史数量增长而变重。
- 轮询仍会定时刷新任务和历史，SSE 已存在但尚未完全接管实时状态刷新。
- SQLite 目前缺少面向任务、图片、会话查询的显式索引。
- 上传参考图时未在前端或后端主动压缩大尺寸图片。
- 静态资源和媒体仍由 FastAPI 进程直接返回，公网高并发下不如 Nginx/CDN 稳。

## 3. 优化优先级

### P0：建立基线指标

目的：先知道当前慢在哪里，避免盲改。

实施内容：

- 增加简单的接口耗时日志，记录路径、状态码、耗时、响应大小。
- 记录媒体文件请求的文件大小、命中路径和响应状态。
- 在前端用浏览器 Network 面板记录首屏加载、历史页、图库页、分镜任务页的请求数量和流量。

验收标准：

- 能看到首页、历史页、图库页、会话详情、任务详情的请求数量和总传输量。
- 能区分 API 慢、图片大、静态文件慢还是上游 AI 慢。

### P1：缩略图与中等图链路

目的：这是降低图片流量的最高收益项。

实施内容：

- 后端新增图片派生能力，为生成图和上传图创建：
  - `thumb`：用于列表、图库、参考图、分镜小图，建议最长边 480px。
  - `medium`：用于普通预览加载前或详情图，建议最长边 1280px。
  - `original`：仅用于原图预览和下载。
- 新增依赖 `Pillow`，安装时必须走项目内 `backend/.venv` 和国内镜像源。
- SQLite `images` 表增加向前兼容字段：
  - `thumb_url`
  - `medium_url`
  - `width`
  - `height`
  - `byte_size`
- 旧数据不覆盖、不重写原图，只在首次访问或维护脚本中补生成派生图。
- 前端所有列表位优先使用 `thumb_url`，预览弹窗优先显示 `medium_url` 后再允许切换原图，下载仍使用原图。

验收标准：

- 图库、历史、会话图片列表的网络传输量明显下降。
- 原图下载文件名、中文标题、预览弹窗能力不退化。
- 多访问密码数据空间之间的缩略图不能串用。

### P2：API 瘦身与分页

目的：减少 JSON 体积和首屏阻塞。

实施内容：

- `/api/tasks` 默认只返回任务列表必要字段，不返回完整 `response/raw`。
- `/api/gallery` 改为分页返回：
  - 默认返回会话分组摘要和每组前几张缩略图。
  - 点击详情再加载该会话完整图片列表。
- 会话详情按需加载图片：
  - 默认加载最近消息和缩略图。
  - 老消息或大量图片使用“加载更多”。
- 前端历史页、图库页和任务详情避免重复请求同一批数据。

验收标准：

- 任务列表接口响应体保持轻量，不随单个任务原始响应暴涨。
- 图库图片很多时，首屏请求数量和 JSON 大小仍可控。
- 点击“查看详情”后再加载完整数据。

### P3：SSE 接管实时状态，降低轮询

目的：减少公网请求数量和后端无效查询。

实施内容：

- 当前有运行任务且 SSE 可用时，减少全局轮询频率。
- `refreshTasks` 改为按需触发：
  - 页面首次进入时触发。
  - SSE 结束事件触发。
  - 用户手动刷新触发。
  - SSE 断开兜底触发。
- 历史和图库不再每 1.8 秒固定刷新，改为任务完成后精准刷新相关会话或分组。
- 对话/分镜工作台只刷新当前会话相关数据，避免全量刷新历史。

验收标准：

- 任务运行期间前端仍能实时显示进度。
- Network 面板中定时 API 请求数量明显下降。
- SSE 断开时仍能靠兜底轮询恢复。

### P4：SQLite 索引与查询优化

目的：历史数据变大后保持稳定。

实施内容：

- 启动迁移中创建缺失索引，禁止覆盖业务数据：
  - `idx_tasks_status_updated_at`
  - `idx_tasks_conversation_created_at`
  - `idx_images_task_id`
  - `idx_images_message_id`
  - `idx_images_conversation_created_at`
  - `idx_conversations_updated_at`
- 优化图库聚合 SQL，优先按会话取摘要，再按需取图片列表。
- 任务、会话、图片查询尽量只选择需要字段。

验收标准：

- 千级图片、百级任务下，历史页和图库页查询仍稳定。
- 更新脚本只做 `create index if not exists`，不破坏云服务器旧数据。

### P5：上传前压缩与参考图优化

目的：减少用户上传大图带来的流量和上游请求体积。

实施内容：

- 前端上传图片前使用浏览器 Canvas 生成压缩副本：
  - 参考图最长边默认 1600px。
  - 蒙版和需要精确编辑的目标图可保留更高尺寸。
- 后端保存上传图时也生成 `thumb` 和 `medium`。
- 在编辑模式继续保留“直接修改目标图 / 辅助参考图”的用途区分。

验收标准：

- 普通参考图上传体积明显下降。
- 直接修改目标图仍能保持足够质量。
- 对话、编辑、分镜模式上传行为一致且可追踪。

### P6：Nginx 静态与媒体分发

目的：降低 FastAPI 进程的文件传输压力。

实施内容：

- `frontend/dist/assets` 由 Nginx 直接返回，开启 Brotli/Gzip 和长期缓存。
- `/media/...` 可继续经 FastAPI 鉴权，也可采用 Nginx `X-Accel-Redirect`：
  - FastAPI 负责鉴权和租户路径校验。
  - 鉴权通过后交给 Nginx 发送真实文件。
- 若后续接 CDN，必须采用私有缓存或签名访问，不能绕过访问密码。

验收标准：

- 静态文件和图片下载不再占用大量 Python 响应时间。
- 未登录用户仍不能直接访问媒体资源。
- 不同访问密码对应的数据空间不能通过缓存串读。

### P7：前端渲染层优化

目的：图片多、历史多时减少浏览器压力。

实施内容：

- 图库和历史列表支持分页或虚拟滚动。
- 图片卡片保持稳定尺寸，避免图片加载时布局跳动。
- 预览弹窗只加载当前图片和相邻图片，不预取整组原图。
- 分镜镜头多时只渲染可见区域或折叠已完成镜头。

验收标准：

- 大会话、大图库下滚动不卡顿。
- 首屏 DOM 节点数量可控。
- 图片预览体验不退化。

## 4. 推荐实施顺序

第一批建议优先做：

1. P1 缩略图与中等图链路。
2. P3 降低轮询，SSE 接管实时刷新。
3. P4 SQLite 索引。

第二批继续做：

1. P2 API 瘦身和图库分页。
2. P5 上传前压缩。
3. P6 Nginx 静态和媒体分发。

第三批体验增强：

1. P7 虚拟滚动和预取策略。
2. 更细粒度的性能面板或管理员诊断页。

## 5. 风险与注意事项

- 缩略图不能替代原图，下载和最终预览必须仍能拿到原图。
- 旧数据迁移必须向前兼容，只能补列、补索引、补派生文件，禁止覆盖已有 SQLite 业务数据。
- CDN 或 Nginx 缓存不能绕过访问密码。
- 多租户数据空间下，缩略图和中等图必须跟随当前访问密码隔离。
- 前端减少轮询后，必须保留 SSE 断线兜底机制。

## 6. 验收清单

- 首页首屏请求量减少，静态资源能命中缓存。
- 历史页和图库页默认只加载缩略图。
- 分镜任务运行中不重复展示规划和图片，镜头图原位更新。
- 大量历史图片下，图库页首屏 JSON 不随总图片数线性暴涨。
- 停止、重试、刷新页面、切换会话后，任务状态仍可恢复。
- 未登录状态下不能直接访问静态资源、媒体资源和业务 API。
- 不同访问密码之间的数据库、上传图、输出图、缩略图和中等图互不串用。
