# 项目全局规范-变更记录

## 2026-05-13

- 变更类型：新增
- 变更项：自动化测试入口约定
- 变更摘要：新增后端自动化测试必须使用 `pytest` 且放在 `tests/`；前端端到端测试必须使用 Playwright 且放在 `frontend/tests/e2e`；前端端到端测试脚本必须先构建前端再执行测试。
- 变更原因：本次为鉴权隔离、provider 池、任务状态机、会话串行、planner 上下文、编辑输入、SSE 和前端关键流程新增自动化测试，需要固化长期测试入口。

- 变更类型：新增
- 变更项：自检脚本访问验证约定
- 变更摘要：新增 `scripts/self_check.sh` 在访问受保护 API 或媒体资源前，必须先完成项目访问验证并携带有效访问 cookie。
- 变更原因：统一密码页启用后，未登录自检会被 `/api/*` 鉴权拦截并误报服务不可用，需要把自检脚本鉴权流程固化为长期运行约束。

- 变更类型：新增
- 变更项：运行产物 Git 提交约定
- 变更摘要：新增运行日志、本地访问 cookie、租户业务数据目录和 SQLite 业务数据禁止提交到 Git，必须通过 `.gitignore` 保持未跟踪。
- 变更原因：本次提交前发现本地运行日志、访问 cookie 和租户数据目录处于未跟踪状态，需要固化为长期仓库卫生规则，避免敏感或环境数据误入 GitHub。

## 2026-05-11

- 变更类型：新增
- 变更项：图片级标签收藏与批量下载约定
- 变更摘要：新增图片记录必须支持图片级收藏状态与标签字段；图库必须支持按会话、任务、图片标签和图片收藏状态批量打包下载，并允许自定义压缩包内文件夹名称，下载包内需保留清单信息。
- 变更原因：本次新增图片批量下载能力，且下载筛选必须基于图片本身的标签和收藏状态，而不能借用提示词收藏等其它语义。

- 变更类型：修改
- 变更项：设置页与模式工作台参数归属约定
- 变更摘要：明确设置页只承载通用设置和可复用资源管理；对话、分镜、普通生图和编辑模式特有的模型、图片参数、高级选项、批量编排与当前任务一致性选择必须放在对应模式工作台中配置，并允许各模式自由选择。
- 变更原因：用户要求重构设置功能，避免所有模式共用一套“当前选择”造成串扰，让每个模式的运行参数回到各自工作台。

- 变更类型：新增
- 变更项：前端文字悬浮提示浮层约定
- 变更摘要：新增“前端所有文字悬浮提示必须使用统一浮层渲染，层级高于普通卡片和内容区，并按视口自动避让边缘与鼠标位置；禁止被父级 `overflow` 裁剪、被卡片遮挡或被鼠标遮住主体文字”的规则。
- 变更原因：本次修复设置项说明框和按钮提示在卡片内被裁剪、遮挡或贴近鼠标导致不可读的问题，需要固化为后续所有提示框实现的统一标准。

## 2026-05-10

- 变更类型：修改
- 变更项：任务阶段 checkpoint 持续刷新约定
- 变更摘要：将任务断点规则从“关键步骤后立即落库 checkpoint”细化为“只要任务仍未完成且阶段、进度、provider 状态或错误状态发生变化，就必须同步刷新最新 checkpoint”。
- 变更原因：本次继续完善失败任务续跑能力，需要保证任务在任意中断点都保留最近一步状态，而不是只在少数关键节点保存。

- 变更类型：修改
- 变更项：失败任务原地续跑约定
- 变更摘要：新增“失败或已停止任务的重试必须优先复用原任务记录、原任务 id、原输入资源和已保存 checkpoint 继续执行；禁止默认新建重试副本冒充续跑”的规则。
- 变更原因：用户明确要求失败后通过重试按钮继续原任务，而不是生成一条新的重试任务记录，需将该交互和数据语义固化为长期标准。

## 2026-05-08

- 变更类型：新增
- 变更项：设置页独立主视图约定
- 变更摘要：新增“设置页面必须作为与工作台、历史、图库并列的独立主视图展示，禁止继续把完整设置区作为常驻侧边栏入口承载”的规则。
- 变更原因：本次把设置区从侧边栏重构为独立页面，需要把页面层级与交互入口统一为长期标准，避免后续界面回退。

- 变更类型：新增
- 变更项：provider 全不可用自动入队重试约定
- 变更摘要：新增“普通生图、编辑、对话和分镜任务若因池内所有 provider 全部暂不可用而中断，必须先保存 checkpoint，再转入同一套定时任务队列按 30 分钟、1 小时、2 小时、5 小时依次自动重试；超出次数后才最终失败”的统一规则。
- 变更原因：本次新增 provider 全不可用场景的后台自动续跑能力，需要把固定退避节奏、checkpoint 保留方式以及并发受控的重试机制固化为项目长期标准。

- 变更类型：修改
- 变更项：失败任务重试与 checkpoint 续跑约定
- 变更摘要：将失败任务重试范围从“普通生图、编辑、分镜”扩展为“普通生图、编辑、对话、分镜”，并新增“关键步骤完成后必须立即落库 checkpoint，重试时优先按最后一个持久化 checkpoint 继续”的统一约定。
- 变更原因：本次完善了多模式失败任务的断点续跑能力，需要把“逐步保存进度、按进度重试”固化为长期统一标准，避免任务意外中断后只能从头开始。

- 变更类型：新增
- 变更项：编辑模式历史用户图去重约定
- 变更摘要：新增“从历史记录进入编辑模式并把历史用户图转换为本轮上传图时，前端必须保留稳定来源标识；同一张图禁止在编辑区和同会话候选图区被当成两次独立选择结果”的规则。
- 变更原因：本次修复了历史用户图进入编辑模式后在候选图区重复可选的问题，需要固化为前端长期一致性约束。

## 2026-05-06

- 变更类型：新增
- 变更项：静态资源与媒体资源缓存约定
- 变更摘要：新增已登录后的前端静态资源和媒体资源必须使用浏览器私有缓存，媒体响应必须带 `Vary: Cookie`，禁止因缓存绕过访问密码或串用不同密码的数据空间。
- 变更原因：云服务器公网访问会反复加载历史图片，需在不影响访问速度和既有鉴权隔离的前提下降低公网流量。

- 变更类型：新增
- 变更项：图片列表懒加载约定
- 变更摘要：新增图片列表、参考图列表和图库缩略图必须优先使用懒加载与异步解码；预览弹窗和下载仍加载原图。
- 变更原因：历史图片数量增长后，页面初次打开不应一次性拉取所有大图，需减少无效流量并保持查看原图能力。

- 变更类型：新增
- 变更项：provider 容错记录展示约定
- 变更摘要：新增任务详情必须保留并展示 provider 重试、不可用与最终成功记录；即使自动切换后任务成功，也必须能追溯渠道容错过程。
- 变更原因：用户要求确认公网访问下多渠道任务是否具备稳健容错，成功任务也需要可见的渠道接力证据，而不只在失败时展示错误。

- 变更类型：新增
- 变更项：分镜规划单点展示约定
- 变更摘要：新增分镜模式工作台内同一任务的分镜规划只能展示一份；镜头运行状态和已生成图片必须在该规划的对应镜头位置原位更新，禁止在运行进度区或图片列表中重复展示同一份规划或同一组结果图。
- 变更原因：分镜连续生图期间每生成一张图会触发任务刷新，若规划和图片在多个区域重复渲染，会造成前端显示冗余并降低镜头进度可读性。

## 2026-05-05

- 变更类型：修改
- 变更项：访问密码配置约定
- 变更摘要：明确访问密码必须支持 8-32 位字母数字组合，并将默认可用访问密码扩展为 `hhs54666` 与 `hhs666666`；`hhs666666` 必须使用独立数据空间。
- 变更原因：用户要求新增访问密码 `hhs666666`，该密码长度超过旧实现的 8 位限制，需要同步调整长期访问密码约束。

- 变更类型：新增
- 变更项：访问用户管理权限约定
- 变更摘要：新增访问用户管理功能仅允许主账号 `hhs54666` 使用和可见；其它访问密码账号禁止查看、新建、修改或删除用户。
- 变更原因：用户要求新增用户管理能力，但该能力只能由主账号使用，必须固化为长期权限边界。

- 变更类型：新增
- 变更项：图片编辑模式多图用途约定
- 变更摘要：新增图片编辑模式传入多张图片时，必须显式区分“直接修改目标图”和“辅助参考图”；后端调用 image_generation 时必须按该用途传入图片说明，禁止把所有输入图混成无差别参考图。
- 变更原因：本次排查发现普通编辑链路多图输入缺少目标/参考语义，会导致 AI 无法稳定判断用户具体要改哪张或哪几张图片。

## 2026-05-04

- 变更类型：新增
- 变更项：生图提供商不可用自动重试与切换约定
- 变更摘要：新增最终生图若命中的 provider 暂不可用，必须先在该 provider 上自动重试 1 次；若仍失败，则自动切换到下一个可用且最优先的 provider。若剩余 provider 只是通道已满，则任务进入等待并在空闲后继续；仅当池内 provider 全部暂不可用时，才允许直接失败并返回明确原因。
- 变更原因：用户要求把 provider 不可用、provider 满载和 provider 全部失效三种情况严格区分处理，并确保多 provider 生图池能自动接力而不是过早失败。

- 变更类型：新增
- 变更项：生图提供商重试/切换前端反馈约定
- 变更摘要：新增当前任务若发生 provider 重试、provider 切换、等待空闲通道或 provider 全部不可用，前端必须通过任务阶段、任务详情或提供商状态区实时反馈给用户，禁止只在最终失败时一次性暴露结果。
- 变更原因：用户要求在多 provider 调度过程中及时看到“正在重试”“正在切换”“正在等待”这类关键状态，降低任务长时间运行时的不确定感。

- 变更类型：新增
- 变更项：设置项悬浮说明框约定
- 变更摘要：新增前端所有可配置设置项的标题区域都必须支持站内悬浮说明框，说明框必须使用不透明文本框展示设置作用、适用场景和推荐设置方式，禁止仅依赖浏览器原生 `title`。
- 变更原因：用户要求在设置面板中把每个设置的用途和设置建议讲清楚，降低误配成本，并保持桌面端与移动端统一的可读反馈体验。

## 2026-05-03

- 变更类型：新增
- 变更项：图片中文命名与下载一致性约定
- 变更摘要：新增对话模式和分镜模式的 AI planner 在生成生图提示词时必须同步生成中文图片名称，保存、历史展示和下载都统一使用该名称；普通生图和编辑模式则优先使用用户填写名称，未填写时自动使用“生成时间 + 会话标题”，多图追加稳定序号；旧数据若无名称则前端显示为空，禁止拿文件名冒充标题。
- 变更原因：用户需要让图片在历史、图库、预览和下载时都能保持可识别的中文命名，同时避免旧数据文件名混入标题造成体验混乱。

- 变更类型：新增
- 变更项：访问密码即数据隔离账号约定
- 变更摘要：新增访问密码不仅控制进入项目，还必须决定其对应的数据空间；不同密码登录后，数据库、上传图、输出图、提供商配置、应用设置、任务、会话、消息、提示词和图片记录都必须自动隔离；现有历史数据默认绑定到 `hhs54666`，后续新增密码需落到独立目录与数据库。
- 变更原因：用户要求把访问密码升级为账号边界，确保不同密码之间完全隔离历史数据与配置，同时保留现有 `hhs54666` 的全部历史数据不丢失。

- 变更类型：新增
- 变更项：分镜提示词预览与镜头上限约定
- 变更摘要：新增分镜模式单任务默认最多生成 20 张图，前端设置和后端校验都必须支持 1-100 范围；同时要求分镜规划完成后先展示计划生成总张数和逐镜头生图提示词列表，提示词默认单行折叠，点击后展开，再按顺序实时展示已生成图片。
- 变更原因：用户需要在分镜正式生图前先核对全部镜头提示词，并支持更大的镜头数量配置范围，以兼顾长视频首帧规划和实时确认体验。

- 变更类型：新增
- 变更项：历史记录模式筛选约定
- 变更摘要：新增历史记录页必须展示每条记录所属的生图模式，并支持按模式筛选历史记录列表。
- 变更原因：历史记录已同时承载对话、分镜、生成和编辑等多种模式，不加模式筛选会降低定位效率，也与图库页现有筛选体验不统一。

- 变更类型：新增
- 变更项：普通生图与编辑模式会话化执行约定
- 变更摘要：新增普通生图模式和图片编辑模式也必须归属到各自独立会话；同一会话内允许连续提交多次请求，但每次都必须直接按用户当次完整提示词执行，禁止调用 planner 生成助手回复，也禁止自动扩写提示词后再生图。
- 变更原因：用户要求普通生图和编辑模式在保留会话归属的同时，执行链路必须保持“直接生图/直接编辑”，不能被对话式 AI 回复和提示词扩写污染。

- 变更类型：新增
- 变更项：失败上下文过滤与失败任务重试约定
- 变更摘要：新增对话/分镜失败后继续原会话时，planner 上下文只能包含用户消息、已完成助手回复和成功图片提示词，禁止带入失败原因、未完成占位内容和失败生图提示词；同时普通生图、编辑和分镜在失败或取消后必须支持基于原请求参数和原输入图重试，并继续遵守同一会话单任务约束。
- 变更原因：避免失败任务的错误信息和未完成内容污染后续对话上下文，同时补齐普通生图与编辑失败后无法原请求重试的问题。

- 变更类型：新增
- 变更项：设置分组显式保存反馈约定
- 变更摘要：新增设置面板中的每个独立设置分组都必须提供单独“保存修改”按钮，并在按钮上方原位展示成功或失败反馈，禁止继续依赖静默自动保存。
- 变更原因：用户需要明确知道当前修改是否已经真正落库，避免设置改动只在本地状态生效却没有被可靠持久化。

- 变更类型：新增
- 变更项：图库模式标识与筛选约定
- 变更摘要：新增图库页必须展示每个会话图片分组所属的生图模式，并支持按模式筛选分组结果。
- 变更原因：历史图片按会话聚合后，仍需要快速区分它们属于对话、分镜、生成还是编辑模式，降低查找成本。

- 变更类型：新增
- 变更项：继续改跨会话引用与底部参考图预览约定
- 变更摘要：新增从历史记录或图库点击“继续改”时，若目标图片不属于当前会话同模式的可复用生成图，前端必须将该图片转换为本轮上传参考图后再进入处理界面；同时工作台底部必须展示已选参考图的真实缩略图预览。
- 变更原因：避免跨会话继续改时仅保存图片 id 导致后端被当前会话过滤后失效，也避免用户只看到“已选”文字却看不到具体参考图内容。

- 变更类型：新增
- 变更项：项目统一密码页访问约定
- 变更摘要：新增项目访问必须先通过统一密码页验证；未完成验证前，禁止直接访问首页、前端静态资源、媒体资源和业务 API。
- 变更原因：项目已要求增加统一访问密码保护，需要确保所有入口都先经过同一套验证，而不是只拦首页不拦接口或静态资源。

- 变更类型：新增
- 变更项：图库按会话聚合展示约定
- 变更摘要：新增图库页必须按会话聚合同一会话的全部历史图片；当单个会话图片过多时，默认仅展示部分预览图，并提供“查看详情”入口展开全部图片；图库预览切换必须在当前会话图片集合内进行。
- 变更原因：保证历史图片浏览时不会把同一会话拆散到多个分组中，方便用户按会话连续查看和在预览弹窗中前后切换。

- 变更类型：新增
- 变更项：全局生图提供商池与单提供商并发约定
- 变更摘要：新增最终生图执行必须统一走全局生图提供商池；普通生图、编辑、对话、分镜与分镜重试共享同一池；默认每个 provider 最多同时运行 3 个生图任务；调度必须优先空闲 provider，其次选择运行中任务最少的 provider；任务与前端界面必须展示当前 provider 和池统计。
- 变更原因：项目已接入多个 GPT 提供商，需要在不突破单 provider 并发限制的前提下提升整体生图吞吐，并让用户可见当前 provider 使用情况与池容量。

- 变更类型：新增
- 变更项：会话模式隔离与运行锁定约定
- 变更摘要：新增对话模式与分镜模式必须使用各自独立会话；同一会话存在 `queued` 或 `running` 任务时禁止继续发送新消息；从历史记录继续对话时必须复用原 `conversation_id` 与原上下文。
- 变更原因：避免同一会话在任务运行期间继续追加消息导致上下文串扰，也避免聊天与分镜混用同一会话破坏历史语义和继续对话行为。

- 变更类型：新增
- 变更项：Git 提交信息中文化约定
- 变更摘要：新增 Git 提交信息必须使用中文；提交标题必须为中文短标题，提交正文必须使用中文详细描述本次改动的核心内容与目的。
- 变更原因：统一项目提交记录的阅读体验与追溯方式，降低后续排查和协作时的语言混杂成本。

- 变更类型：新增
- 变更项：分镜逐张图片实时展示约定
- 变更摘要：新增分镜连续生图每完成并保存一张镜头图片后，后端必须立即通过任务级 SSE 推送镜头状态和图片信息，前端必须立即展示该图片，禁止等全部镜头完成后才统一刷新。
- 变更原因：分镜任务耗时较长，用户需要按镜头顺序实时看到每张已完成图片和当前进度，便于确认连续性并降低等待不确定性。

## 2026-05-02

- 变更类型：新增
- 变更项：云服务器更新脚本数据库保护约定
- 变更摘要：新增项目更新脚本禁止覆盖、重置或重写云服务器已有 SQLite 业务数据；若版本涉及表结构变化，只允许执行向前兼容的 schema 迁移，例如创建缺失表或补充缺失列。
- 变更原因：云服务器数据库承载长期历史任务、会话、图片和 provider 配置，项目更新必须保护业务数据，同时保证代码升级后的表结构兼容。

- 变更类型：新增
- 变更项：AI planner 接口格式兼容约定
- 变更摘要：新增 AI planner 默认使用 `/v1/responses`，仅在用户显式选择时允许切换为 `/v1/chat/completions`；`chat/completions` 仅用于文本规划，不得影响 image_generation 生图执行链路。兼容模式只保证传入文字对话、参考图文字说明和已知生图提示词。
- 变更原因：兼容通义、DeepSeek 等只支持或更适合 `chat/completions` 的文本规划模型，同时确保现有 Responses 生图功能不受影响。

- 变更类型：新增
- 变更项：前端图片预览交互约定
- 变更摘要：新增图片预览必须使用站内固定大弹窗，支持原图展示、拖拽移动、缩放，以及同一任务、会话或图库分组内上一张/下一张切换，禁止默认打开新页面。
- 变更原因：提升历史图片、任务图片和对话图片的查看效率，避免预览图片时跳出当前工作流。

- 变更类型：新增
- 变更项：任务级 SSE 实时反馈约定
- 变更摘要：新增前端必须通过任务级 SSE 事件流实时接收对话模式和分镜模式的助手回复、任务状态与完成事件；轮询只能作为兜底刷新。
- 变更原因：用户要求真正前端实时逐字反馈，需要用浏览器事件流承接后端 planner stream，而不是仅依赖定时轮询数据库。

- 变更类型：新增
- 变更项：对话与分镜 planner 回复流式更新约定
- 变更摘要：新增对话模式和分镜模式的 AI planner 回复必须支持流式更新；任务启动后先创建助手占位消息，并在 planner 流式返回过程中持续更新该消息内容。
- 变更原因：提升对话和分镜模式的即时反馈体验，避免用户必须等待 planner 完整返回后才看到 AI 回复。

- 变更类型：新增
- 变更项：同一会话参考图选择稳定性约定
- 变更摘要：新增同一会话内已选参考图不得因发送对话或分镜消息自动取消；候选参考图片可随新生成图片增加，但不得自动改变已选参考图。
- 变更原因：连续对话和分镜制作需要稳定使用用户明确选中的参考图，避免每次发送后丢失参考约束或被新增候选图自动替换。

- 变更类型：新增
- 变更项：AI planner 与生图执行解耦约定
- 变更摘要：新增对话模式和分镜模式必须支持 planner 与 image_generation 使用不同模型和不同 provider；planner 只负责对话、意图判断和提示词撰写，image_generation 负责实际生图执行。
- 变更原因：支持使用其它渠道模型完成对话与提示词规划，同时保留 GPT 图像链路执行实际生图。

- 变更类型：新增
- 变更项：单张图片提示词约定
- 变更摘要：新增 AI planner 输出的生图提示词必须只对应一张图片；若参考图是用户本轮上传且没有历史生图提示词，禁止编造该参考图的对应提示词。
- 变更原因：避免 planner 将提示词写成说明、流程、多图列表，或错误地为用户上传参考图补造不存在的历史提示词。

- 变更类型：新增
- 变更项：对话与分镜 AI 上下文约定
- 变更摘要：新增对话模式和分镜模式传给 AI planner 的上下文只能包含显式文字消息与已生成图片的文字提示词；选择参考图时必须按当前会话过滤，并将参考图图片与对应提示词/说明成对传入，禁止跨会话串用。
- 变更原因：修复不同任务或不同对话选中参考图混入当前请求、以及隐藏上游上下文导致 AI 误读历史图片关系的问题。

- 变更类型：新增
- 变更项：分镜失败追踪与重试约定
- 变更摘要：新增分镜连续生图失败、取消或意外中断时必须在历史记录中保留镜头状态、已完成图片和失败原因，并支持基于已完成镜头继续重试。
- 变更原因：分镜模式按顺序依赖上一镜头输出图，失败后需要可追踪、可续做，避免用户只能重新开始整组镜头。

- 变更类型：新增
- 变更项：分镜连续生图约定
- 变更摘要：新增分镜连续生图必须先规划镜头列表和一致性概述，再按镜头顺序逐张生成；第 2 个及后续镜头必须以上一镜头输出图作为 edit 输入，并保存中文镜头名与顺序。
- 变更原因：本次新增分镜模式，需要长期约束其不能退化为普通并发批量生图或合图，保证人物、场景和逻辑连续性。

- 变更类型：新增
- 变更项：生图参数约定
- 变更摘要：新增默认电脑壁纸生图参数必须为 `2560x1440` + `high`，禁止将 `1536x1024` 标注为电脑横屏 2K；`1536x1024` 仅作为 3:2 稳定横图展示。
- 变更原因：修正前端“电脑横屏 2K 高清”实际对应 `1536x1024` 导致生成结果不符合电脑壁纸尺寸的问题。

- 变更类型：新增
- 变更项：初始化《项目全局规范》
- 变更摘要：建立项目基础技术栈、目录职责、AI 接口约定、数据存储、默认端口和交付文案的最小统一规则。
- 变更原因：项目根目录原先缺少《项目全局规范》与对应变更记录文件，需要先建立唯一可信的规范基线，保证后续产出统一、可追踪。

- 变更类型：新增
- 变更项：上游默认配置一致性约定
- 变更摘要：新增“前端默认 provider、数据库默认 provider 与后端 `.env` 默认上游必须保持一致，禁止存在相互冲突的默认 `base_url`”规则。
- 变更原因：本次排查发现前后端默认上游来源不一致会直接放大生图链路的不稳定与排查歧义，需要补充为统一约束。

- 变更类型：新增
- 变更项：后端运行环境与重启校验约定
- 变更摘要：新增“后端必须使用 `backend/.venv` 启动，且后端代码、依赖或 `.env` 变更后必须重启进程再验证”的规则。
- 变更原因：本次排查确认运行中的后端进程未使用项目虚拟环境，且磁盘代码版本与常驻进程版本可能脱节，已对生图稳定性与排查准确性造成影响。

- 变更类型：新增
- 变更项：生图 SDK 调用层与容错约定
- 变更摘要：新增“Responses 生图调用层必须优先使用 OpenAI SDK，并支持瞬时失败统一重试与 stream 失败回退非 stream”的规则。
- 变更原因：项目目标是将 `imagegen` skill 的稳定生图逻辑产品化，必须把 SDK、重试和流式回退能力作为长期统一约束。

- 变更类型：新增
- 变更项：角色档案与风格锁定存储约定
- 变更摘要：新增“角色档案和风格锁定必须作为可复用资源单独持久化到 SQLite，并允许在普通生图、图片编辑、对话生图和分镜任务中显式绑定使用”的规则。
- 变更原因：本次新增角色档案与风格锁定功能，需要把长期一致性资源从一次性表单输入升级为可复用、可追踪的项目级能力。

- 变更类型：新增
- 变更项：批量变体与定时任务追踪约定
- 变更摘要：新增“批量变体任务必须记录变体名称、定时执行时间、批次标签和调度顺序；定时任务在页面刷新后仍必须可恢复、可继续执行”的规则。
- 变更原因：本次新增定时执行与批量变体能力，需要统一其持久化字段和历史追踪方式，避免后续任务不可追溯。

- 变更类型：新增
- 变更项：定时批量任务串行调度约定
- 变更摘要：新增“同一会话下的定时任务和批量变体任务必须按创建顺序串行调度；只有前一个任务结束后，后一个任务才允许进入实际执行”的规则。
- 变更原因：同一会话任务共享上下文和输入约束，必须明确调度顺序，避免批量任务互相抢占导致会话状态混乱。
