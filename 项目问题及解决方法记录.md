# 项目问题及解决方法记录

## 2026-05-02 运行中的后端进程未使用项目虚拟环境，且代码版本与常驻进程脱节

### 问题现象
- `8010` 端口上的实际后端进程使用的是系统 `Anaconda Python`，不是项目内 `backend/.venv`。
- `backend/app/main.py` 的磁盘修改时间晚于常驻后端进程启动时间，导致“代码已改，但运行结果仍是旧行为”。

### 影响
- 不同解释器中的依赖版本、模块行为和错误表现可能不一致。
- 排查数据库历史时看到的请求参数和当前磁盘代码不一致，容易误判为代码无效或逻辑混乱。

### 解决方法
- 启动或重启后端时统一使用：`backend\\.venv\\Scripts\\python.exe backend\\run.py`
- 每次修改后端代码、后端依赖或 `.env` 后，必须重启常驻后端进程，再做功能验证。
- 验证时先检查 `8010` 端口对应进程的解释器路径与启动时间，确认与当前代码版本一致。

---

## 2026-05-02 第三方网关在 `/v1/responses + image_generation` 链路上存在明显不稳定

### 问题现象
- 最近失败记录中同时出现了 `502 upstream_error`、`524 timeout`、`429 rate_limit_exceeded`。
- 文本 planner 请求往往成功，但真正进入 `image_generation` 阶段后更容易失败。

### 影响
- 项目表现为“有时能生图、有时突然失败”，用户会误以为前端或提示词本身有问题。
- 对话模式一次请求需要先走 planner、再走 image generation，本身比直接生图多一次 `/responses` 调用，更容易触发限流和网关超时。

### 解决方法
- 若需求已经明确，优先走直接 `generate` / `edit`，减少对话模式额外的一次 `/responses` 调用。
- 将并发控制在较低水平，并保留自动重试；若仍频繁出现 `524/502/429`，优先更换更稳定的上游 provider。
- 排查时优先区分“planner 成功、image_generation 失败”和“整个请求都失败”，不要把两者混为同一个问题。

---

## 2026-05-02 项目实现尚未真正对齐 `imagegen` skill 的核心容错逻辑

### 问题现象
- `imagegen` skill 使用 OpenAI SDK、流式优先提取最终图、流式失败回退非流式，并对 `429/502/503/504/upstream_error/timeout` 做统一重试。
- 当前项目自行实现了 `httpx + /responses` 调用层，默认重试次数更少，且稳定重试主要只覆盖网关超时场景。

### 影响
- 在相同上游下，skill 可恢复的瞬时错误，项目侧更容易直接暴露为失败。
- 项目与 skill 虽然都走 `/v1/responses`，但稳定性、报错质量和恢复能力并不等价。

### 解决方法
- 若项目目标是“把 skill 逻辑产品化”，优先复用或严格对齐 skill 的请求与容错策略，而不是重新实现一套更弱的调用层。
- 至少应统一以下能力：`base_url/api_key` 解析来源、流式最终图提取、流式失败回退非流式、对瞬时上游错误的统一重试策略。
- 在完成逻辑对齐前，不要把“项目已等价转换了 skill 能力”当作既定事实。

---

## 2026-05-02 Windows 下项目 venv 进程列表显示基础解释器路径

### 问题现象
- 使用 `backend\.venv\Scripts\python.exe backend\run.py` 启动后，Windows 进程列表中端口监听子进程可能显示为创建该 venv 的基础解释器路径。
- 本项目当前 venv 的基础解释器是 `D:\anaconda3\python.exe`，因此端口拥有者可能显示为 Anaconda Python。

### 影响
- 单看 `ExecutablePath` 容易误判服务没有使用项目虚拟环境。
- 若纯 Anaconda 环境缺少项目依赖，但服务仍能启动并导入项目新增依赖，说明服务实际使用的是 venv 的包环境。

### 解决方法
- 重启时仍以 `backend\.venv\Scripts\python.exe backend\run.py` 作为唯一启动命令。
- 验证时同时检查：启动父进程命令行、venv 中依赖是否存在、纯 Anaconda 中依赖是否不存在、服务是否能加载新增依赖后的接口。

---

## 2026-05-02 前端“电脑横屏 2K 高清”标签与实际像素不一致

### 问题现象
- 前端将“电脑横屏 2K 高清”配置为 `1536x1024`，该尺寸是 3:2，不是常见电脑壁纸 16:9。
- 数据库任务参数和实际输出图片都显示 `1536x1024`，说明前端和后端传参正常，但选项标签误导了用户。

### 影响
- 用户选择“电脑 2K 高清”后，生成结果会像 3:2 横图，而不是 `2560x1440` 电脑壁纸。
- 旧版保存的表单设置会继续带着 `1536x1024`，导致升级后仍可能沿用旧尺寸。

### 解决方法
- 将默认电脑壁纸尺寸统一为 `2560x1440` + `high`。
- 将 `1536x1024` 改名为“横屏 3:2 稳定”，禁止再标注为电脑横屏 2K。
- 对旧版未带 `settings_version` 的 `1536x1024` 保存设置做一次迁移，加载时自动切回新的默认电脑 2K 尺寸。

---

## 2026-05-02 分镜连续生图不能使用普通批量生图

### 问题现象
- 视频分镜首帧需要人物、服装、场景、光线和动作逻辑连续。
- 如果直接把多个镜头当作普通批量生图并发提交，每张图之间没有上一镜头画面作为约束，容易出现人物漂移、场景跳变和叙事断裂。

### 影响
- 用户会得到一组主题相近但不连续的图片，无法作为视频镜头首帧序列使用。
- 失败或中断时也难以判断已经完成到哪个镜头。

### 解决方法
- 分镜模式必须先由 AI 规划人物概述、场景概述和镜头列表。
- 第 1 镜头从文本或用户显式参考图生成；第 2 镜头开始必须把上一镜头输出图作为 edit 输入，再结合当前镜头提示词生成。
- 每个镜头生成后立即写入任务进度与镜头状态，失败时保留已完成镜头并标记失败镜头，便于续做和排查。

---

## 2026-05-02 对话参考图跨会话串台

### 问题现象
- 前端切换不同对话或任务后，已选参考图状态可能仍保留在当前表单里。
- 后端如果只按图片 id 查询参考图，不校验当前会话，会把其它对话中的图片传给当前 planner。

### 影响
- AI 会误以为当前需求引用了其它对话的图片，导致人物、场景或编辑目标串台。
- 分镜模式中参考图与提示词关系一旦混乱，会破坏后续镜头连续性。

### 解决方法
- 前端切换会话时必须清空或按当前 `conversation_id` 过滤已选参考图。
- 后端加载选中参考图时必须同时校验当前 `conversation_id`。
- 发送给 planner 时，参考图必须和对应提示词/说明成对排列，并用稳定 `ref` 标识二者关系。

---

## 2026-05-02 同一会话内参考图被发送消息自动清空

### 问题现象
- 对话或分镜消息提交成功后，前端会清空 `chatReferenceImages`。
- 用户明明想在同一会话内持续用同一张人物或场景参考图，下一轮对话却失去参考约束。

### 影响
- 连续改图和分镜首帧制作会出现人物、场景或风格约束突然消失。
- 新生成图片加入候选列表后，用户难以判断当前到底还选中了哪些参考图。

### 解决方法
- 同一会话内发送消息后只清空本轮上传文件，不自动清空已选历史参考图。
- 已选参考图只在用户手动取消、切换/新建/删除会话时清理或按当前会话过滤。
- 候选参考图片列表可以随新生成图片增加，但不能自动替换或取消用户已选参考图。

---

## 2026-05-02 Windows 本地无法直接执行 Bash 自检脚本

### 问题现象
- 在 Windows 本地执行 `bash scripts/self_check.sh` 时，`bash` 可能被解析到 WSL。
- 若本机未启用 WSL/Hyper-V，会在进入项目脚本前失败，并出现 `HCS_E_HYPERV_NOT_INSTALLED`。

### 影响
- 该失败只能说明当前 Windows Bash/WSL 环境不可用，不能直接判断 `scripts/self_check.sh` 或云服务器一键脚本有问题。
- 本地测试云服务器脚本时，容易把环境限制误判为项目脚本错误。

### 解决方法
- 云服务器或已安装 Git Bash/WSL 的环境中继续使用 `bash scripts/self_check.sh`。
- Windows 本地若未启用 WSL，应使用等价 PowerShell 检查：项目 venv 编译、`/api/health`、前端首页、`/api/settings`、`/api/providers`、`/api/app-settings`。
- 如需完整验证 Bash 脚本链路，先安装可用的 Git Bash 或启用 WSL/Hyper-V，再重新执行脚本。

---

## 2026-05-02 更新脚本不应覆盖云服务器业务数据库

### 问题现象
- 旧版 `update_project.sh` 在更新时会加载 `.env` 并调用 `configure_env.sh`。
- `configure_env.sh` 会初始化 SQLite，并更新第一条 provider 记录为 `.env` 中的默认上游配置。
- `start_background.sh` 也曾在检测到 `IMAGE_API_KEY` 环境变量时自动调用 `configure_env.sh`，导致更新后启动仍可能覆盖 provider。

### 影响
- 云服务器长期积累的 provider、settings、任务、会话、消息、提示词和图片记录存在被更新流程改写的风险。
- 用户在前端维护的 provider 配置可能被 `.env` 默认值覆盖，造成生图线路突然变化。

### 解决方法
- 更新项目时禁止调用会写业务数据的 `configure_env.sh`。
- 更新流程如需同步数据库变化，只能运行 schema 迁移脚本，执行创建缺失表、补充缺失列等向前兼容操作。
- `start_background.sh` 默认不得自动配置 `.env` 或 provider；只有显式设置 `AUTO_CONFIGURE_ENV=1` 时才允许执行配置初始化。

---

## 2026-05-03 分镜图片只在全部完成后刷新

### 问题现象
- 分镜模式按镜头顺序逐张生成图片，但旧逻辑主要在任务完成或轮询刷新时更新前端图片列表。
- 单个镜头图片保存后，后端没有单独推送“该镜头图片已保存”的事件，前端当前对话消息和任务详情也不会立即追加这张图。

### 影响
- 长分镜任务执行时，用户看不到每个镜头完成后的即时成果，容易误判任务卡住。
- 若后续镜头失败，用户需要刷新历史详情才更容易看到已完成镜头图片。

### 解决方法
- 每保存一张分镜镜头图片后，后端必须立即发布包含 task、shot、image 的任务级 SSE 事件。
- 前端收到事件后，必须立刻更新当前任务详情和当前对话助手消息的图片列表。
- 任务快照应携带已保存图片，保证历史详情、任务抽屉和 SSE 重连后状态一致。

---

## 2026-05-03 分镜重试首镜头时丢失原始参考图

### 问题现象
- 分镜任务首次执行时，第 1 镜头可以使用用户上传图或已选历史图作为参考。
- 进入 `/api/tasks/{task_id}/retry` 后，重试逻辑只会回收已完成镜头的输出图作为 `previous_image`，不会重新装载原任务的输入参考图。
- 如果失败点就在第 1 镜头，或需要从没有已完成镜头的状态重新开始，重试会退化成纯文本 `generate`。

### 影响
- 首镜头重试结果会失去角色/场景锚点，人物外观和场景细节容易漂移。
- 用户会误以为“继续试一次”是在原参考条件下重跑，实际却已经换了约束条件。

### 解决方法
- 分镜重试必须同时保存并恢复原任务的输入参考图集合，而不只恢复已完成镜头输出图。
- 若第 1 镜头原本依赖参考图，重试第 1 镜头时必须继续走 `edit` 并把原始参考图重新传给 image_generation。
- 任务参数或独立表中应显式保存“首镜头参考图来源”和“后续镜头连续编辑来源”，避免重试时靠临时推断。

---

## 2026-05-03 分镜长链只依赖上一帧会逐步漂移

### 问题现象
- 分镜主链路在第 2 镜头及后续镜头中只把“上一镜头输出图”作为 edit 输入。
- 第 1 镜头使用过的角色设定图、场景参考图、道具参考图不会继续参与后续镜头执行。
- 文本里虽然保留了人物/场景概述，但图像级锚点已经只剩最近一帧。

### 影响
- 镜头数一长，人物脸型、服装细节、场景布局和光线风格会累计漂移。
- 一旦某一帧轻微跑偏，后续镜头会把偏差继续放大，难以作为稳定的视频首帧序列。

### 解决方法
- 后续镜头除上一帧外，还应允许持续带上固定角色锚点、场景锚点或关键道具锚点。
- 至少要支持“上一帧 + 固定角色参考图/设定图”的双层约束，而不是只保留单帧链式传递。
- 对长分镜应支持关键帧重锚定策略，例如每隔 N 个镜头重新注入角色设定图或用户指定参考图。

---

## 2026-05-03 分镜完成后把扩写版连续性提示词回灌进上下文，导致上下文膨胀

### 问题现象
- planner 先产出每个镜头的原始 prompt。
- 执行阶段又把“人物概述 + 场景概述 + 连续性要求 + 原始镜头 prompt”拼成更长的 `continuity_prompt`，并回写到 `shot.prompt`。
- 后续再次规划同一对话时，历史上下文会把这些扩写后的长 prompt 全量塞回 planner。

### 影响
- 对话越长、镜头越多，上下文会快速膨胀，增加 token 开销和 planner 输出不稳定性。
- planner 读到的是“执行期补强后的长提示词”，不再是原始分镜意图，容易造成信息重复、自我污染和规划跑偏。

### 解决方法
- 分离保存“planner 原始镜头 prompt”和“执行期 continuity_prompt”，禁止互相覆盖。
- 构造后续 planner 上下文时，优先回传原始镜头意图摘要，而不是执行期扩写全文。
- 对历史分镜上下文增加长度治理，例如只保留镜头摘要、关键连续性字段和用户确认过的不变量。

---

## 2026-05-03 分镜 SSE 断线或刷新后不会自动恢复实时事件流

### 问题现象
- 前端只在新建任务成功后启动 `EventSource`。
- 页面刷新、重新进入对话、或 SSE 连接临时报错后，前端没有为现存运行任务重新订阅事件流。
- 当前实现一旦触发 `onerror` 就直接关闭连接，后续主要依赖轮询补状态。

### 影响
- 分镜生成过程中的逐镜头完成通知会丢失实时性，用户只能靠轮询或手动刷新看到更新。
- 断线瞬间完成的镜头图片不一定能及时回灌到当前消息区域，体验会从“流式连续生成”退化成“间歇刷新”。

### 解决方法
- 页面初始化、切回正在执行的对话、或刷新后，必须自动为当前运行中的任务重建 SSE 订阅。
- `EventSource` 发生错误时不应直接永久关闭；应做指数退避重连，并在重连后用快照补齐当前状态。
- 任务级 SSE 快照除了任务状态外，还应能补发当前助手消息和已完成镜头图片，避免重连后界面缺图。

---

## 2026-05-03 继续改如果只保存历史图片 id，会在跨会话或非生成图场景下失效

### 问题现象
- 从历史记录或图库点击“继续改”时，前端若只把图片 id 填进已选参考图状态，而不把图片转成当前轮次的上传参考图，后端发送时仍会按当前会话过滤可选参考图。
- 生成模式、编辑模式、图库入口或模式切换后的继续改，常常不在原会话上下文里，导致图片虽然显示“已选”，实际提交时可能被后端过滤掉。
- 工作台底部若只显示“已指定 1/3 张”之类的文字，用户无法确认当前到底选中了哪一张图片。

### 影响
- 跨会话继续改会出现“前端看起来已选中，但真正发送时图片没带上”的隐性失败。
- 生成模式和图库里的继续改体验不稳定，用户很难判断当前是否真的把目标图带进了本轮任务。
- 只显示文字不显示缩略图，会放大误操作和排查成本。

### 解决方法
- 仅当目标图片属于当前会话同模式下可直接复用的生成图时，才允许用历史图片 id 作为参考图。
- 其它继续改场景必须把目标图片转换成当前轮次的上传参考图，确保提交时不依赖跨会话查询。
- 工作台底部必须显示已选参考图的真实缩略图、用途和移除入口，不能只显示数量或文字提示。

---

## 2026-05-03 只拦首页不拦接口和静态资源，会让密码页形同虚设

### 问题现象
- 项目若只在前端首页加一个密码弹窗，但不统一拦截 API、媒体资源和静态资源，用户仍可直接访问接口或资源地址。
- 前端单独做密码判断时，刷新页面、直接打开图片地址或访问 API 路径，都可能绕过页面层限制。

### 影响
- 访问控制只停留在界面层，无法真正保护项目内容和业务接口。
- 用户以为项目“加了密码”，但实际上接口、图片和前端资源仍可被直接访问，存在明显安全漏洞。

### 解决方法
- 访问控制必须统一放到后端入口层，对首页、静态资源、媒体资源和业务 API 做同一套登录态校验。
- 登录页应使用独立轻量 HTML 输出，避免在未登录时还依赖受保护的前端构建资源。
- API 未登录时返回统一未授权响应；页面访问未登录时跳转到密码页，确保所有入口行为一致。

---

## 2026-05-03 同一会话在任务运行中继续发消息会导致上下文串扰

### 问题现象
- 同一个 `conversation_id` 在已有 `queued` 或 `running` 任务时，前端仍可继续发送新消息。
- 对话模式与分镜模式也可能共用同一个会话，导致聊天与分镜历史混在一起。
- 从历史记录点击“继续对话”时，预期应该回到原会话继续，但切模式或再次发送后容易被错误地当成新会话或混入其它模式。

### 影响
- 同一会话在任务未完成时继续追加消息，会让任务状态、助手占位消息、参考图和上下文历史互相串扰。
- 聊天与分镜混用同一会话后，planner 读取到的历史语义不再单一，后续生成结果更容易跑偏。
- 历史记录里的“继续对话”会失去“继续原上下文”的意义，排查和追踪都会变得困难。

### 解决方法
- 对话模式与分镜模式必须使用各自独立的会话，不允许在同一 `conversation_id` 内混用。
- 同一会话存在 `queued` 或 `running` 任务时，前后端都必须阻止继续发送新消息；用户只能先停止该会话任务，或新建会话后继续。
- 从历史记录继续对话时，必须复用原 `conversation_id` 与原上下文；只有在用户主动切换到另一种模式时，才新建对应模式的新会话。

---

## 2026-05-03 图库如果按任务或时间碎片化分组，会打断同一会话的浏览连续性

### 问题现象
- 图库里的历史图片没有稳定按 `conversation_id` 聚合，导致同一会话生成的图片可能被拆到多个小分组里。
- 当同一会话图片较多时，列表会被整段撑长，用户很难快速扫到该会话的整体内容。
- 预览入口如果只绑定当前可见图片子集，上一张和下一张就无法覆盖同一会话的全部图片。

### 影响
- 用户难以把同一会话里的生成结果当作一个连续过程查看，尤其是对话连续改图或分镜任务。
- 图库页会因为单个会话图片过多而显得冗长，浏览效率下降。
- 预览弹窗里的前后切换范围不稳定，会出现“列表里明明还有图，但弹窗切不到”的割裂体验。

### 解决方法
- 图库分组必须优先按 `conversation_id` 聚合同一会话的全部历史图片；无会话归属的图片再按任务或单图兜底分组。
- 单个会话图片过多时，列表默认只显示部分预览缩略图，并提供“查看详情”展开全部图片。
- 图片预览时必须始终传入该会话的完整图片数组，而不是只传当前可见子集，确保上一张和下一张能覆盖同一会话全部图片。

---

## 2026-05-03 单全局并发限制会拖慢多提供商生图吞吐

### 问题现象
- 项目虽然支持配置多个 GPT 提供商，但最终生图执行仍按整个项目共用一个固定并发上限。
- 当某个 provider 已达到自身并发限制时，其它空闲 provider 也无法被优先利用。
- 用户在前端看不到当前任务究竟落到了哪个 provider，也无法判断池内还有多少 provider 空闲。

### 影响
- 多 provider 无法真正分摊生图压力，整体吞吐受最慢或最拥挤的单 provider 限制。
- 同时运行多个任务时，容易出现“还有 provider 空闲但总任务仍在排队”的体感问题。
- 排查限流、速度波动和 provider 稳定性时，缺少可见的 provider 使用信息，定位成本高。

### 解决方法
- 建立全局生图提供商池，普通生图、编辑、对话、分镜和分镜重试统一从池中选择最终执行 provider。
- 并发上限改为“单 provider 默认最多 3 个运行中任务”，项目总容量按“池内 provider 数 × 3”计算。
- 调度时先挑当前没有运行生图任务的 provider；如果所有 provider 都在运行任务，再选择运行中任务数最少的 provider。
- 任务记录、任务详情和前端提供商管理区都要展示当前实际使用的 provider 以及生图池总数、已使用数、空闲数。
- 前端在发起任务前必须先把最新 provider 池设置写回后端，避免刚调整池配置就提交任务时仍命中旧 provider 池。

---

## 2026-05-03 多参考图执行时缺少语义角色标注，容易引用错图

### 问题现象
- 分镜和对话模式最多可传 3 张参考图。
- 真正发给 image_generation 执行层时，每张图都只被标成通用的 `Input image N: primary reference image`。
- 执行层不知道哪张是角色设定、哪张是场景、哪张是服装或道具锚点。

### 影响
- 多参考图场景下，模型容易混淆各张图的职责，出现人物特征串用、场景误融合或风格污染。
- 分镜首镜头一旦引用错图，后续“以上一帧继续 edit”会把错误持续传递下去。

### 解决方法
- planner 或后端需要为每张参考图补充明确语义，例如“角色脸部锚点”“服装锚点”“场景锚点”。
- 执行层构造 input 时应把每张参考图的用途一起传给模型，而不是统一叫 primary reference image。
- 前端选择多参考图时也应允许用户标注用途，减少执行层猜测空间。

---

## 2026-05-04 生图提供商“不可用”和“满载等待”必须分开处理

### 问题现象
- 只有把 provider 是否暂不可用和 provider 是否只是通道已满区分开，才能决定任务是重试、切换、等待还是直接失败。
- 之前如果只看“当前 provider 失败”，容易把本来还能等待恢复的任务直接判死，或者把已经坏掉的 provider 继续分配给后续任务。

### 影响
- 用户会看到不符合实际的失败提示，误以为整个生图池都坏了。
- 任务调度容易在“切换 provider”和“等待空闲槽位”之间来回摇摆，造成前端状态不稳定。

### 解决方法
- 为 provider 增加短期不可用冷却状态，失败后先在当前 provider 上重试一次，再自动切换到下一个可用 provider。
- 若其它 provider 只是满载，则任务进入等待状态，等有空闲槽位再继续。
- 若池内 provider 全部处于不可用状态，则直接失败，并把每个 provider 的不可用原因返回给前端。

---

## 2026-05-03 仅挂载 `/assets` 时，Vite `public` 根资源不会被后端自动托管

### 问题现象
- 当前后端只显式挂载了 `frontend/dist/assets`，而 Vite `public` 目录产物会输出到 `dist` 根目录。
- 例如 `favicon.svg` 虽然已经构建到 `frontend/dist/favicon.svg`，但访问 `/favicon.svg` 时若没有单独路由，后端会落到前端兜底 `index.html`，浏览器标签图标实际不会正确返回。

### 影响
- 标签页图标、根路径级静态文件或后续其它 `public` 资源会出现“构建出来了，但线上访问拿到的是 HTML”的隐性故障。
- 密码页即使写了 `<link rel=\"icon\">`，如果资源路由没补齐，未登录页和已登录页都看不到正确图标。

### 解决方法
- 对所有需要从站点根路径访问的 `public` 资源，后端必须显式提供对应文件路由，不能假设 `/assets` 挂载会自动覆盖它们。
- 当前项目至少需要为 `/favicon.svg` 和 `/favicon.ico` 提供独立路由，并在未登录放行白名单中同步加入这些路径。

---

## 2026-05-03 历史会话若只依赖 `meta.uploads` 恢复图片，会丢失输入图语义

### 问题现象
- 历史消息中的 `meta.uploads` 只保存了文件路径，未直接保存 `input`、`mask`、`input_reference` 等来源语义。
- 如果前端只按 `meta.uploads` 还原 `uploaded_images`，编辑模式候选图区会把蒙版、用户上传图、历史生成参考图都混成普通图片，甚至把历史生成参考图误标为用户图。

### 影响
- 编辑模式候选图区和底部参考图预览的“用户图 / 生成图 / 蒙版”标识会失真。
- 蒙版可能错误出现在可选编辑输入里，历史生成参考图也可能被当成普通上传图，影响继续改和同会话候选图的稳定性。

### 解决方法
- 历史消息恢复 `uploaded_images` 时，必须优先结合 `images` 表中的同消息非 `api` 图片记录还原真实 `source` 和标题，而不是只信任 `meta.uploads`。
- 对 `reference_image_ids` 恢复出的历史生成参考图，还要保留 `origin_source=api` 等原始来源信息，前端标签判断优先使用原始来源而不是仅看 `input_reference` 外壳。

---

## 2026-05-04 planner 选图结果若不落到执行层，会导致“知道要改哪张图”与“实际改哪张图”脱节

### 问题现象
- planner 虽然可以输出“本次用了哪些参考图、哪几张是直接修改目标图”，但如果执行层仍把全部候选图一起传给 image_generation，最终生图就会忽略 planner 的精确选图结果。
- 分镜链路如果默认裁掉部分参考图、或首镜头只要带参考图就强制走 edit，也会让“辅助锚点图”和“直接修改目标图”混淆。

### 影响
- 用户同时选择多张图时，系统可能错误修改不该改的图片，或把本应只是角色/场景参考的图片当成直接编辑底图。
- 历史记录里看起来像是“AI 选对了图”，但实际执行时并没有按选图结果传参，问题排查会被误导。

### 解决方法
- planner 输出中必须区分“全部使用到的参考图”和“直接修改目标图”，执行层只能按该结果组装最终输入。
- 首镜头是否走 `generate` 还是 `edit` 必须单独判定；有参考图不等于一定要 edit。
- 分镜和对话链路都要把直接修改目标图排在前面，把辅助锚点图单独标注后再传给 image_generation，禁止再把所有候选图当成同一种 `primary reference image`。

---

## 2026-05-05 参考图“直接修改/辅助参考”选择若不持久化，会在重试或恢复时退化

### 问题现象
- 用户已经在对话或分镜里把参考图明确标记为“直接修改目标”或“辅助参考”。
- 如果任务参数里的 `seed_images` 只保存图片路径、角色和说明，不保存 `selection_mode`，第一次执行后续看起来可能正常，但分镜重试、任务恢复或历史继续时，这层意图会丢失。
- 执行层重新读取 `seed_images` 后，只能把这些图都当成普通参考图，无法继续稳定识别哪些图应该被直接修改。

### 影响
- 用户明明已经说清楚“改哪张图”，重试后系统却又回到“不确定改哪张”的状态。
- 分镜首镜头和后续继续重试时，容易把本应直接编辑的图片误降级为辅助锚点图。

### 解决方法
- `seed_images` 序列化时必须同时保存 `selection_mode`。
- 从任务参数恢复候选参考图时，必须同步恢复 `selection_mode` 和对应标签。
- 执行层在 planner 没有明确回填 edit target、但用户已经显式标记了“直接修改目标”时，应把该显式标记作为强意图参与最终执行判定。

---

## 2026-05-05 API Key 若混入中文标点，不能让底层编码异常直接暴露给用户

### 问题现象
- 用户从聊天软件或文档复制 API Key 时，末尾很容易混入中文句号、中文逗号或全角空格。
- 之前这类值会一路带到 `Authorization` 请求头，最终在底层编码阶段报出 Python 的 `ascii` 编码异常。

### 影响
- 前端和任务记录只能看到“失败”，但错误信息不直观，用户很难第一时间意识到是 Key 里混进了中文标点。
- Provider 保存、默认设置保存和真实任务执行都可能踩中同一类问题，排查成本高。

### 解决方法
- 在保存默认 API Key、保存 provider API Key 以及真正发起上游请求前，都要校验 Key 是否包含非 ASCII 字符。
- 一旦发现中文句号、中文逗号、全角空格等字符，必须直接返回明确中文提示，指导用户删除这些字符后重试。

---

## 2026-05-05 普通编辑多图必须显式区分直接修改目标和辅助参考

### 问题现象
- 普通编辑模式若只把多张图片统一作为 `images` 上传，后端无法知道用户想直接修改哪一张或哪几张。
- 即使对话/分镜已经支持 `edit_target` 与 `reference`，普通编辑链路若不传同类语义，最终 `image_generation` 仍会把所有输入图混成普通参考图。

### 影响
- 用户同时上传目标图、角色图、风格图时，AI 可能错误修改辅助参考图，或没有稳定保留真正目标图。
- 编辑任务失败重试时，如果没有把每张输入图的用途写入任务参数，重试会丢失“要改哪几张”的语义。

### 解决方法
- 普通编辑前端也必须提供“直接修改 / 辅助参考”选择，并随 `params_json.upload_selection_modes` 提交。
- 后端编辑链路必须按该字段生成 `input_image_notes`，把直接修改目标图排在前面，辅助参考图单独标注后传给 `image_generation`。
- 编辑任务参数必须持久化 `upload_selection_modes`，重试时必须按原输入图顺序恢复同样的用途语义。

---

## 2026-05-05 新增访问密码时必须同步检查长度校验

### 问题现象
- 历史默认访问密码是 8 位，旧实现用正则固定限制为 8 位字母数字。
- 新增 `hhs666666` 这类 9 位密码时，如果只加入默认密码列表而不改校验规则，启动后该密码会被过滤掉，表现为“配置了但永远登录失败”。

### 影响
- 新密码无法进入系统，也不会创建独立数据空间。
- 云服务器更新后容易误判为部署或缓存问题，增加排查成本。

### 解决方法
- 访问密码校验必须允许 8-32 位字母数字组合。
- 新增默认密码时必须同时确认默认密码集合、环境变量解析和校验正则三者一致。

---

## 2026-05-06：公网后台任务 provider 等待与 SSE 反馈稳健性

- 问题：任务在等待 provider 空闲通道时若被取消，内存中的 provider `assigned_count` 可能未释放，导致后续任务误判通道繁忙。
- 影响：云服务器长时间运行后，用户停止排队任务可能逐步污染 provider 池状态，造成任务排队、切换和容量展示不准确。
- 解决方法：在获取 provider semaphore 的等待段增加取消/异常清理逻辑；若等待或进入运行计数前被中断，必须释放 semaphore 并回退 `assigned_count` / `running_count`。
- 问题：上游生图 stream 事件只更新数据库，未立即推送任务级 SSE。
- 影响：公网访问时前端实时反馈依赖轮询兜底，无法稳定做到“上游开始处理、局部预览、最终图片返回”等状态即时展示。
- 解决方法：`handle_image_stream_event` 与 `handle_storyboard_stream_event` 每次有效更新任务状态后必须调用 `publish_task_snapshot`，让 SSE 与数据库状态同步。

---

## 2026-05-06：分镜规划与图片结果重复展示

- 问题：分镜模式中助手消息会展示分镜规划，运行任务气泡也会展示同一份动态规划；每生成一张图后，图片又在规划下方按普通图片列表重复展示。
- 影响：分镜生成期间前端会出现多份相同规划和结果图，用户难以判断当前镜头进度，也会误以为 AI 重复规划。
- 解决方法：分镜工作台只保留助手消息中的一份规划；任务 SSE 和轮询刷新时把最新 storyboard 状态同步回该助手消息；每张生成图按 `image_id/url/title` 匹配并原位插入对应镜头节点，运行任务气泡只展示总体进度。

---

## 2026-05-07 图片列表默认拉原图会放大公网流量

- 问题：历史图库、任务列表和会话图片如果直接默认加载原图，即使浏览器做了懒加载，公网访问时仍会产生很高的图片流量。
- 影响：首屏请求体积大、预览加载慢、云服务器出网流量被快速消耗，图片越多越明显。
- 解决方法：统一增加 `thumb / medium / original` 三层图片链路，列表与卡片默认只用缩略图，预览再切中图或原图；同时把图库改成“会话/任务分组摘要 + 按需展开明细”，避免首屏一次性拉全量图片。

---

## 2026-05-08 长任务未逐步保存 checkpoint 会导致失败后只能从头重跑

- 问题：普通生图、编辑、对话和分镜任务若只在最终完成时写数据库，一旦服务重启、任务失败或进程中断，就无法可靠知道 planner 是否已完成、已保存了几张图片、分镜执行到了哪个镜头。
- 影响：失败任务即使已经完成了部分工作，也只能从头重新规划或重新生成，浪费额度、时间和上下文连续性。
- 解决方法：在 planner 完成、单张图片保存、单镜头保存和最终整理等关键步骤后立即写入 `checkpoint_json`；重试时优先恢复已完成图片和已完成 planner 结果，再从最后一个持久化步骤继续执行。

---

## 2026-05-08 编辑模式把历史用户图转为当前上传图后，若丢失来源 key 会在候选图区重复可选

- 问题：从历史记录点击用户上传图进入编辑模式时，若转换后的当前上传文件没有保留与历史图片一致的来源标识，前端“同会话候选图”区域无法识别它已经加入编辑区。
- 影响：同一张图会一边出现在“编辑图片”区，一边又在“同会话候选图”里显示为“加入编辑”，用户再次点击后会把同一图片重复加入一次。
- 解决方法：历史图片转换为当前上传文件时，必须同步保留稳定的 `sourceImageKey/sourceImageLabel`；候选图区选中态统一基于该来源 key 判断，而不是只看当前 `File` 对象本身。

---

## 2026-05-08 定时/自动重试调度若只读取 tasks 裸行，会丢失 images 与 checkpoint 上下文

- 问题：定时任务分发器如果只从 `tasks` 表读取基础字段，而不补齐任务关联的 `images` 和 `checkpoint_json`，恢复执行时就拿不到输入图、已完成输出图和断点进度。
- 影响：编辑任务可能找不到原始输入图，自动重试无法基于已完成图片续跑，分镜/对话任务也会丢失已规划或已执行到一半的上下文。
- 解决方法：所有“继续执行已有任务”的入口都必须优先加载带图片与 checkpoint 的完整任务视图，再决定是 fresh run、断点续跑还是失败重试；禁止直接拿裸任务行进入调度执行。

---

## 2026-05-10 同一任务 id 续跑时，运行中任务注册表若无实例校验会误清掉新任务

- 问题：当失败/取消任务改为复用原任务 id 继续执行后，旧运行协程的清理回调如果只按 `task_id` 直接删除 `RUNNING_TASKS`，会把新一轮同 id 任务也一并清掉。
- 影响：原任务刚点击“重试”后，新的续跑协程可能被旧协程回调误判为已结束，导致取消状态串写、运行句柄丢失、后续停止/状态跟踪异常。
- 解决方法：任务运行注册表的清理逻辑必须同时校验“当前映射的实例是否就是本次完成/取消的协程”；只有实例一致时才允许清理运行句柄或回写取消状态，禁止只凭 task id 做清理。

---

## 2026-05-11 原任务重试若清空旧响应但回滚不完整，会留下半重置任务状态

- 问题：失败任务原地重试时，为了避免前端继续读取旧结果，通常会先清空 `response_json`；但如果后续调度启动失败，回滚逻辑若没有把旧 `response_json` 一起恢复，任务会停留在“状态已回滚、历史结果却丢失”的半重置状态。
- 影响：用户点击重试失败后，任务详情可能失去上一轮结果上下文，前端展示也会与真实历史不一致。
- 解决方法：所有“先清空旧状态再尝试重试”的链路，都必须在回滚快照里同时包含 `response_json`、`checkpoint_json`、状态、错误和 provider 字段；一旦调度失败，必须原样恢复整组旧字段。

---

## 2026-05-11 任务早期失败时若要求先有 checkpoint 才能合并状态，会丢失第一份恢复上下文

- 问题：有些任务会在第一份正式 checkpoint 生成前就发生错误，例如定时调度起步失败、参数恢复失败、输入资源缺失等；如果状态合并逻辑只允许在“已有 checkpoint”前提下写回，就会让这类早期失败没有任何断点元数据。
- 影响：任务虽然在表里显示 failed，但恢复层拿不到 `mode / stage / last_status / last_error`，后续排查和重试依据会缺失。
- 解决方法：checkpoint 合并函数必须支持按任务当前模式和状态自动补建最小基础 checkpoint，再叠加最新阶段、错误和恢复字段；禁止把“没有旧 checkpoint”当作放弃记录状态的理由。

---

## 2026-05-10 会话任务恢复若不复用断点快照并排除当前消息，会导致上下文丢失或重复

- 问题：对话/分镜任务在服务重启、手动重试或自动恢复时，如果不复用 `checkpoint_json + seed_images`，而是重新从实时数据库拼参考图和历史消息，就可能出现参考图静默缺失、当前用户请求被重复塞进 planner 上下文、分镜 planner 阶段无法继续恢复等问题。
- 影响：模型实际拿到的背景上下文与原始任务不一致，轻则理解漂移，重则直接无法继续编辑或续跑分镜，表现为“明明点了重试，但 AI 像忘了刚才的图和要求”。
- 解决方法：会话任务恢复时必须优先复用已持久化的 `seed_images` 快照与 checkpoint；若快照缺图或引用图不再可用，必须显式报错，禁止静默少传；重新构造 planner 历史时必须排除当前任务对应的用户消息和未完成助手消息，避免同一请求被重复注入上下文；服务重启后未完成任务应转入可调度恢复态，而不是直接标记失败。

---

## 2026-05-11 前端悬浮提示框被卡片裁剪或被鼠标遮住

- 问题：设置项说明框若使用组件内部绝对定位，容易被 `.settingsGroup` 等父级 `overflow` 裁剪；按钮若使用浏览器原生 `title`，提示位置不可控，可能贴近鼠标导致文字不可读。
- 影响：用户查看设置说明、图标按钮含义或截断文本时，提示框会被其它卡片覆盖、裁掉，或被鼠标遮住关键信息。
- 解决方法：统一使用固定定位的 portal 浮层渲染文字提示，设置足够高的 `z-index`，根据目标元素位置自动在视口内上下避让；交互元素禁止继续依赖原生 `title` 作为主要提示实现。

---

## 2026-05-11 图片批量下载若复用提示词收藏或只按前端当前列表打包，会导致筛选语义不可靠

- 问题：图片下载需要按会话、任务、标签和收藏状态筛选；如果没有图片级标签/收藏字段，或只把前端当前已加载缩略图列表拿来打包，会漏掉未展开会话中的图片，也会把提示词收藏误当作图片收藏。
- 影响：用户按收藏或标签下载时结果不完整、不准确，且页面刷新后标签/收藏状态可能丢失。
- 解决方法：在 `images` 表中持久化图片级 `favorite` 与 `tags`；批量下载必须由后端按数据库筛选原图文件并生成 ZIP，前端只负责提交筛选条件和自定义文件夹名称。

---

## 2026-05-11 模式运行参数放在通用设置页会造成跨模式选择串扰

- 问题：模型、图片参数、高级选项、批量编排和当前任务一致性选择如果都放在通用设置页，用户会误以为这些配置是全局固定值；切换模式后也容易沿用上一模式的选择。
- 影响：对话、分镜、生成、编辑四种模式的运行参数互相干扰，尤其是风格锁和角色档案会被当作统一选择，而不是当前模式当前任务的显式绑定。
- 解决方法：设置页只保留通用配置与资源库管理；每个模式工作台必须展示并保存自己的运行参数入口，一致性控制在当前模式内自由选择，提交任务时按当前模式表单值传给后端。

---
---

## 2026-05-13：本地运行产物未忽略会在提交前混入工作区

- 问题：本地服务日志、访问 cookie、租户业务数据目录如果没有进入 `.gitignore`，提交前会以未跟踪文件出现在工作区，容易被误提交到 GitHub。
- 影响：运行日志和 cookie 可能包含环境信息或登录状态，租户业务数据目录可能包含数据库、上传图和生成图，误提交会带来隐私、体积和环境污染风险。
- 解决方法：将 `backend/local-server*.log`、`frontend/local-frontend*.log`、`storage/codex-cookie.txt`、`storage/tenants/` 等运行产物加入 `.gitignore`；提交前使用 `git status --short` 复核只剩代码、文档和必要配置变更。

---

## 2026-05-13：启用统一密码页后，自检脚本直接访问 API 会误报服务不可用

- 问题：项目中间件会拦截未登录的 `/api/*` 请求；自检脚本若直接 `curl /api/health`、`/api/settings` 等接口，会收到 401 而不是健康检查结果。
- 影响：真实服务已经启动时，部署自检仍可能失败，导致更新流程误判为服务不可用。
- 解决方法：自检脚本必须先向 `/auth/login` 提交有效访问密码，保存 cookie 后再携带 cookie 访问首页、设置接口、provider 接口和其它受保护资源；自检密码允许通过 `ACCESS_SELF_CHECK_PASSWORD` 覆盖。

---

## 2026-05-13：同一轮定时调度需要记录已派发会话，避免同会话任务并发启动

- 问题：定时任务分发器在同一轮循环中调度多个 scheduled 任务时，异步任务可能尚未来得及把数据库状态改为 running，后续同会话任务就看不到前序任务已经被派发。
- 影响：同一会话下多个定时任务或批量变体任务可能同时启动，破坏同会话串行和上下文稳定性。
- 解决方法：调度循环内维护本轮已派发的 `conversation_id` 集合；如果后续任务属于同一会话，即使数据库暂未出现 running 任务，也必须保持 scheduled 并显示“等待同会话前序任务完成”。

---

## 2026-05-13：SSE 重连只回放终态事件时，事件流需要立即关闭

- 问题：任务级 SSE 连接断开后重连，如果服务器只从快照中回放 `done/failed/canceled` 终态事件，而没有进入队列等待阶段的终态关闭逻辑，客户端流会持续挂起。
- 影响：前端或 E2E 测试在任务已完成后重连事件流可能无法自然结束，导致等待超时；真实用户也可能保留无意义的长连接。
- 解决方法：`/api/tasks/{task_id}/events` 在回放快照时若遇到 `done/failed/canceled`，必须在 yield 该事件后立即结束响应。

---

## 2026-05-14：`2560x1440` 默认尺寸会触发 OpenAI SDK 类型告警但本项目暂按规范保留

- 问题：OpenAI SDK 对 `image_generation` 工具参数 `size='2560x1440'` 可能输出 `PydanticSerializationUnexpectedValue` 类型告警，原因是 SDK 当前类型枚举未显式包含该尺寸。
- 影响：运行日志会出现序列化告警，后续排查生图问题时容易误判为代码错误；但项目当前全局规范要求默认电脑壁纸参数必须保持 `2560x1440 + high`。
- 解决方法：在用户确认按现有规范执行后，暂不修改默认尺寸与规范；若后续要改为官方 SDK 枚举尺寸，必须先触发规范冲突确认，并同步修订《项目全局规范》和相关前后端默认参数。

---

## 2026-05-14：普通生图和编辑任务的单张失败必须在当前图片序号处落 checkpoint

- 问题：普通生图和图片编辑在每张图执行过程中若上游失败、没有返回图片数据或发生普通异常，只由外层任务状态机记录泛化失败，容易缺少 `current_image_index / completed_count / bucket / provider_attempts` 等失败点信息。
- 影响：失败任务虽然可点重试，但前端无法明确展示“从第几张继续”，后端也可能只能按任务整体重跑，尤其是第一张失败时会被误判为不可续跑。
- 解决方法：直接生图链路的每张图片执行必须包裹失败 checkpoint；失败时写入 `step=image_waiting`、当前图片序号、已完成数量、provider 尝试记录和错误详情，并将 `can_resume=true`，手动重试继续复用原任务 id 与原 checkpoint。

---

## 2026-05-16：checkpoint JSON 不包含阶段信息会导致服务重启恢复丢失最近断点文案

- 问题：任务执行时虽然会把 `stage/progress` 写入任务表字段，但如果 `checkpoint_json` 本体没有同步保存 `stage/progress`，服务重启扫描未完成任务时无法从 checkpoint 还原最近阶段，只能显示默认“检测到未完成任务”。
- 影响：失败点或中断点仍在任务表短期可见，但一旦重启，用户和调度器看到的恢复说明会变模糊，降低“从失败点继续”的可追踪性。
- 解决方法：`persist_task_checkpoint` 写 checkpoint 时必须把本次 `stage/progress` 同步写入 checkpoint JSON；重启恢复测试必须断言未完成任务会转为 `scheduled`，且 checkpoint 中的当前图片序号、阶段和可续跑标记保持不丢。

---

## 2026-05-16：Windows 环境不能依赖 WSL Bash 作为唯一自检入口

- 问题：在 Windows 开发机上执行 `bash scripts/self_check.sh` 可能因 WSL 虚拟化未启用而失败，导致无法验证当前 8010 服务、前端静态产物和核心 API 是否真实可用。
- 影响：项目本身可能正常，但自检入口不可用会让功能稳定性验证中断，尤其是在 Windows 本地维护和部署前检查时。
- 解决方法：保留 Bash 自检用于类 Unix 环境，同时提供 `scripts/self_check.ps1` 作为 Windows 等价入口；PowerShell 自检必须使用项目内虚拟环境、完成访问登录，并检查健康接口、首页、设置、provider 和 app-settings API。

---

## 2026-05-16：图片预览组件禁止保留新窗口兜底

- 问题：图片卡片即使主要路径已经传入站内预览弹窗处理器，如果组件内部仍保留 `window.open(..., "_blank")` 兜底，后续新增调用点忘记传 `onPreview` 时会重新退回浏览器新窗口预览。
- 影响：这会违反项目“图片预览必须使用站内固定大弹窗”的长期交互约定，也会让历史、图库和任务详情的预览体验出现不一致。
- 解决方法：图片卡片只在存在 `onPreview` 时展示预览按钮，禁止内置新窗口兜底；增加前端契约测试，断言前端源码不得出现 `window.open` 和 `target="_blank"`。
